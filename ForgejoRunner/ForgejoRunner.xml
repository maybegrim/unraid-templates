<?xml version="1.0" encoding="utf-8"?>
<Container version="2">
  <Name>ForgejoRunner</Name>
  <Repository>data.forgejo.org/forgejo/runner:6</Repository>
  <Registry>https://code.forgejo.org/forgejo/-/packages/container/runner/latest</Registry>
  <Network>bridge</Network>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://forums.unraid.net/topic/XXXXX-support-forgejo-runner/</Support>
  <Project>https://code.forgejo.org/forgejo/runner</Project>
  <Overview>
Forgejo Runner executes CI/CD workflows for Forgejo Actions, the built-in continuous integration system in Forgejo (a self-hosted Git forge).&#xD;
&#xD;
This template uses host Docker socket mode — the runner dispatches workflow jobs as sibling containers on your Unraid Docker daemon. No Docker-in-Docker or privileged mode needed.&#xD;
&#xD;
SETUP: You must register the runner before starting the container.&#xD;
&#xD;
1. Create the data directory: mkdir -p /mnt/user/appdata/forgejo-runner/data&#xD;
2. Generate config: docker run --rm data.forgejo.org/forgejo/runner:6 forgejo-runner generate-config > /mnt/user/appdata/forgejo-runner/data/runner-config.yml&#xD;
3. Register the runner (see support thread for full command).&#xD;
4. Start this container — the runner will connect and begin picking up jobs.&#xD;
&#xD;
The container must run as root (user 0:0) for Docker socket access on Unraid.
  </Overview>
  <Category>Productivity</Category>
  <WebUI/>
  <TemplateURL>https://raw.githubusercontent.com/maybegrim/unraid-templates/main/ForgejoRunner/ForgejoRunner.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/maybegrim/unraid-templates/main/ForgejoRunner/icon.png</Icon>
  <ExtraParams>--user 0:0</ExtraParams>
  <PostArgs>forgejo-runner daemon --config /data/runner-config.yml</PostArgs>
  <DonateText/>
  <DonateLink/>
  <Requires>A running Forgejo instance with Actions enabled. You must register the runner before first start.</Requires>
  <Date>2026-05-01</Date>

  <!-- ===================== -->
  <!-- CORE SETTINGS (always shown) -->
  <!-- ===================== -->

  <!-- Data Volume — runner state, registration, config -->
  <Config Name="Data Path" Target="/data" Default="/mnt/user/appdata/forgejo-runner/data" Mode="rw" Description="Persistent storage for runner registration (.runner file), config, and cache. Contains the authentication token after registration — do not delete." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/forgejo-runner/data</Config>

  <!-- Docker Socket — host Docker access for running workflow jobs -->
  <Config Name="Docker Socket" Target="/var/run/docker.sock" Default="/var/run/docker.sock" Mode="rw" Description="Host Docker socket. Allows the runner to create sibling containers for workflow jobs. Required for Docker-based job execution." Type="Path" Display="always" Required="true" Mask="false">/var/run/docker.sock</Config>

  <!-- ===================== -->
  <!-- ADVANCED SETTINGS (shown via "Show more settings") -->
  <!-- ===================== -->

  <!-- Docker Host override -->
  <Config Name="DOCKER_HOST" Target="DOCKER_HOST" Default="unix:///var/run/docker.sock" Mode="" Description="Docker daemon endpoint. Default uses the mounted host socket. Change to tcp://host:port if using a remote Docker daemon or Docker-in-Docker." Type="Variable" Display="advanced" Required="false" Mask="false">unix:///var/run/docker.sock</Config>

</Container>
